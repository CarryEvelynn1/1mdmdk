<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guard Bot Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #e67e22;
            --secondary-color: #1e272e;
            --accent-color: #d35400;
            --success-color: #2ecc71;
            --danger-color: #e74c3c;
            --warning-color: #f1c40f;
            --dark-bg: #0f1215;
            --card-bg: #181c22;
            --text-primary: #ecf0f1;
            --text-secondary: #95a5a6;
            --border-color: #2c3e50;
            --fox-glow: 0 0 15px rgba(230, 126, 34, 0.3);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background: linear-gradient(135deg, var(--dark-bg) 0%, #1a1c24 100%);
            color: var(--text-primary);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
        }

        /* Sidebar Code Fox Düzenlemesi */
        #sidebar {
            width: 260px;
            background: var(--card-bg);
            border-right: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
            padding: 20px;
            transition: all 0.3s ease;
            position: fixed;
            height: 100%;
            z-index: 1000;
            box-shadow: 2px 0 20px rgba(0,0,0,0.5);
        }

        .brand-title {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--primary-color);
            margin-bottom: 40px;
            display: flex;
            align-items: center;
            gap: 10px;
            text-shadow: 0 0 10px rgba(230, 126, 34, 0.4);
        }

        .nav-item {
            padding: 12px 15px;
            color: var(--text-secondary);
            text-decoration: none;
            border-radius: 8px;
            margin-bottom: 5px;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .nav-item:hover, .nav-item.active {
            background: rgba(230, 126, 34, 0.15); /* Orange tint */
            color: var(--primary-color);
            transform: translateX(5px);
            border-left: 3px solid var(--primary-color);
        }

        #content {
            flex: 1;
            margin-left: 260px;
            padding: 30px;
            width: calc(100% - 260px);
        }

        .stats-card {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 25px;
            border: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
            transition: transform 0.2s;
        }

        .stats-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary-color);
            box-shadow: var(--fox-glow);
        }

        .stats-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            opacity: 0.8;
        }

        .stats-info h3 { font-size: 2rem; margin: 0; color: var(--text-primary); }
        .stats-info p { margin: 0; color: var(--text-secondary); font-size: 0.9rem; }

        .activity-log {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 25px;
            border: 1px solid var(--border-color);
        }

        .section-title {
            font-size: 1.2rem;
            margin-bottom: 20px;
            color: var(--primary-color);
            font-weight: 600;
            border-bottom: 2px solid var(--border-color);
            padding-bottom: 10px;
            display: inline-block;
        }

        .activity-item {
            padding: 15px;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            gap: 15px;
            align-items: center;
        }
        .activity-item:last-child { border-bottom: none; }
        .activity-icon {
            width: 40px;
            height: 40px;
            background: rgba(230, 126, 34, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-color);
        }

        .sidebar-toggle {
            display: none;
            position: fixed;
            top: 15px;
            left: 15px;
            z-index: 1001;
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            padding: 8px 12px;
            border-radius: 5px;
        }

        @media (max-width: 768px) {
            #sidebar { transform: translateX(-100%); }
            #sidebar.open { transform: translateX(0); }
            #content { margin-left: 0; width: 100%; padding-top: 60px; }
            .sidebar-toggle { display: block; }
        }
        
        .code-fox-badge {
            margin-top: auto;
            text-align: center;
            font-size: 0.7rem;
            color: var(--text-secondary);
            opacity: 0.4;
            padding-top: 20px;
            font-family: 'Orbitron', sans-serif;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>
    <button class="sidebar-toggle" onclick="toggleSidebar()">
        <i class="fas fa-bars"></i>
    </button>

    <div id="sidebar">
        <div class="brand-title">
            <i class="fas fa-shield-cat"></i> Guard Bot
        </div>
        <nav>
            <a href="/dashboard/<%= guildID %>" class="nav-item active"><i class="fas fa-home"></i> Genel Bakış</a>
            <a href="/bot-settings/<%= guildID %>" class="nav-item"><i class="fas fa-robot"></i> Bot Ayarları</a>
            <a href="/guard/<%= guildID %>" class="nav-item"><i class="fas fa-shield-alt"></i> Koruma</a>
            <a href="/safe-users/<%= guildID %>" class="nav-item"><i class="fas fa-user-shield"></i> Güvenli Kişiler</a>
            <a href="/roles/<%= guildID %>" class="nav-item"><i class="fas fa-user-tag"></i> Roller</a>
            <a href="/channels/<%= guildID %>" class="nav-item"><i class="fas fa-hashtag"></i> Kanallar</a>
            <a href="/logs/<%= guildID %>" class="nav-item"><i class="fas fa-history"></i> Log Kayıtları</a>
        </nav>
        <div class="code-fox-badge">
            <i class="fas fa-code"></i> DESIGN BY CODE FOX
        </div>
    </div>

    <div id="content">
        <div class="container-fluid">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="fw-bold" style="color: var(--text-primary);">Sunucu İstatistikleri</h2>
                <div class="user-profile d-flex align-items-center gap-3">
                    <span class="badge bg-warning text-dark"><%= user.username %></span>
                    <a href="/logout" class="btn btn-sm btn-outline-danger"><i class="fas fa-sign-out-alt"></i> Çıkış</a>
                </div>
            </div>

            <div class="row">
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-info">
                            <h3><%= guild.memberCount %></h3>
                            <p>Toplam Üye</p>
                        </div>
                        <i class="fas fa-users stats-icon"></i>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-info">
                            <h3><%= guild.roleCount %></h3>
                            <p>Rol Sayısı</p>
                        </div>
                        <i class="fas fa-user-tag stats-icon"></i>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-info">
                            <h3><%= guild.channelCount %></h3>
                            <p>Kanal Sayısı</p>
                        </div>
                        <i class="fas fa-hashtag stats-icon"></i>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-info">
                            <h3>%<%= guild.protectionLevel || 100 %></h3>
                            <p>Güvenlik Skoru</p>
                        </div>
                        <i class="fas fa-shield-check stats-icon"></i>
                    </div>
                </div>
            </div>

            <div class="row mt-4">
                <div class="col-12">
                    <div class="activity-log">
                        <div class="section-title">
                            <i class="fas fa-bolt me-2"></i> Son Aktiviteler
                        </div>
                        <% if (recentLogs && recentLogs.length > 0) { %>
                            <% recentLogs.forEach(log => { %>
                                <div class="activity-item">
                                    <div class="activity-icon">
                                        <i class="fas fa-info-circle"></i>
                                    </div>
                                    <div class="activity-content">
                                        <div class="activity-title"><%= log.description %></div>
                                        <div class="activity-time" style="color: var(--accent-color); font-size: 0.8rem;">
                                            <%= log.timestamp ? new Date(log.timestamp).toLocaleString('tr-TR') : '-' %>
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <div class="activity-item">
                                <div class="activity-content">
                                    <div class="activity-title text-muted">Henüz aktivite kaydı yok.</div>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('open');
        }

        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const sidebarToggle = document.querySelector('.sidebar-toggle');
            
            if (window.innerWidth <= 768) {
                if (!sidebar.contains(event.target) && !sidebarToggle.contains(event.target)) {
                    sidebar.classList.remove('open');
                }
            }
        });
    </script>
</body>
<%- include('layout-blocker.ejs') %>
</html>
 
